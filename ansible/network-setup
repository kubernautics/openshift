#!/bin/ansible-playbook
- name: Build MSIO Hypervisor OpenShift Network
  hosts: base
  become: true
  gather_facts: yes
  vars_files:
    - vars/custom.yml
    - vars/grub.yml
    - vars/user.yml
  roles:
    - role: openvswitch
      tags: ovs
    - role: libvirt
      tags: virt
    - role: networking
      tags: net
  tasks:
    - hostname:
        name: "{{ ministack_hostname_base }}"
